# Server Environment Configuration
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# Database (Leave blank to use embedded MongoDB Memory Server in development)
# MONGODB_URI=mongodb://localhost:27017/pakistan_lms

# Security & Authentication
JWT_SECRET=super_secret_jwt_key_for_ilmportal_pakistan_2026
JWT_EXPIRE=30d

# SMTP / Email Configuration (Optional - falls back to simulated console logger)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=587
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_password
SMTP_FROM=noreply@pakistanlms.pk
